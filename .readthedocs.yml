# Required
version: 2

# Image to use
build:
  tools: 
    python: "3.9"
  os: ubuntu-22.04
  image: testing

# Configuration
python:
  version: 3.9
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py